/**
 * 1. use form tag and onsubmit handler with event(e) 
 *  to access the form e.target.[name of the input field].value 
 * 
 * 2.controlled element:
 *           use individual field state for each input field
 * 3. controlled element :
 *          one object in a state holding all the input fileds value
 * 4. uncontrolled element:
 *     useRef to create reference and access value by using like: [nameRef.current.value]
 * 5. use hooks to handle states, submit, error . [example react hook form]
 * 6. reusable component for multiple form data changes
 */